version: "1.0.0"
project_types:
  - backend
  - frontend
  - data
  - infra
  - product
description: Phase 5. Deployed feature + criteria + logs → feedback report; human decides hotfix vs new feature.
