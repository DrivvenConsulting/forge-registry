version: "1.0.0"
plan_mode_first: true
prompt_for_inputs_before_run: true
project_types:
  - backend
  - data
  - infra
  - frontend
description: Pipeline from problem framing (idea_shaper) through requirements, channel validation, feasibility; work tracking via Linear (Backlog to Ready).
required_inputs:
  - feature_name
optional_inputs:
  - raw_idea
  - team
  - project
inputs:
  - feature_name
  - raw_idea
  - team
  - project
outputs:
  - artifacts_feature_definitions
  - work_item_linear_status
  - pull_requests_linked_to_work_item
