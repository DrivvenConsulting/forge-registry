name: Claude PR Main Closed

on:
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  run:
    uses: DrivvenConsulting/github-actions-templates/.github/workflows/claude-pr-main-closed.yml@main
    secrets: inherit
