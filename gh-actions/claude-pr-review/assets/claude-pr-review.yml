name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - development

jobs:
  review:
    timeout-minutes: 10
    uses: DrivvenConsulting/github-actions-templates/.github/workflows/claude-pr-review.yml@main
    secrets: inherit
